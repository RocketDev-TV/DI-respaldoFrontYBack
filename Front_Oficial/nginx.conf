server {
    listen 80;
    location / {
        root /usr/share/nginx/html;
        index index.html index.htm;
        # Esto hace que las rutas de React funcionen siempre
        try_files $uri $uri/ /index.html;
    }
}
